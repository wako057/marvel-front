# travelcar 
### (tested only on linux debian)

Fill the `nodejs/src/config-dev.json` with the marvel credentials

Start the docker

`docker-compose up`

Start the angular:

`ng serve --open`

Access the view :

`http://localhost:4200` 
